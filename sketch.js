 function setup() {
    createCanvas(500, 500);
      background("#E91E1E");
      }

      function draw() {
        stroke("#000A11");
          fill("#FFFFFF");
            
                 if(mouseIsPressed) {
                      rect(mouseX, mouseY, 20, 20);
                        }
                        }
